<%= render 'layouts/nav1' %>

<div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 py-12 px-6">
  <div class="max-w-5xl mx-auto bg-white rounded-2xl shadow-2xl p-10">
    
    <!-- Header -->
    <div class="flex flex-col md:flex-row md:items-center md:justify-between mb-10">
      <div>
        <h1 class="text-4xl font-extrabold text-indigo-700">📊 Admin Dashboard</h1>
        <p class="text-lg text-gray-600 mt-2">
          Welcome back, <span class="font-semibold text-gray-800"><%= current_user.username %></span> 👋
        </p>
      </div>
     
    </div>

    <hr class="my-8">

    <!-- Quick Links -->
    <h2 class="text-2xl font-semibold text-gray-800 mb-6">⚡ Quick Links</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      <%= link_to admin_products_path,
        class: "group bg-gradient-to-r from-blue-500 to-indigo-500 text-white font-medium p-6 rounded-xl shadow hover:scale-105 transform transition" do %>
        <div class="text-lg">📦 Manage Products</div>
        <p class="text-sm opacity-80 mt-1 group-hover:opacity-100">View and edit all products</p>
      <% end %>

      <%= link_to new_admin_product_path,
        class: "group bg-gradient-to-r from-green-500 to-emerald-500 text-white font-medium p-6 rounded-xl shadow hover:scale-105 transform transition" do %>
        <div class="text-lg">➕ Add Product</div>
        <p class="text-sm opacity-80 mt-1 group-hover:opacity-100">Create a new product entry</p>
      <% end %>

      <%= link_to "#",
        class: "group bg-gradient-to-r from-purple-500 to-pink-500 text-white font-medium p-6 rounded-xl shadow hover:scale-105 transform transition" do %>
        <div class="text-lg">👥 Manage Users</div>
        <p class="text-sm opacity-80 mt-1 group-hover:opacity-100">Check all registered users</p>
      <% end %>
    </div>

    <hr class="my-10">

    <!-- Stats Section -->
    <h2 class="text-2xl font-semibold text-gray-800 mb-6">📈 Stats Overview</h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
      <div class="bg-white border border-gray-200 rounded-xl p-6 shadow hover:shadow-lg transition">
        <span class="text-sm text-gray-500">Total Products</span>
        <div class="text-3xl font-extrabold text-indigo-600 mt-2"><%= Product.count %></div>
      </div>
      <div class="bg-white border border-gray-200 rounded-xl p-6 shadow hover:shadow-lg transition">
        <span class="text-sm text-gray-500">Total Users</span>
        <div class="text-3xl font-extrabold text-green-600 mt-2"><%= User.count %></div>
      </div>
      <div class="bg-white border border-gray-200 rounded-xl p-6 shadow hover:shadow-lg transition">
        <span class="text-sm text-gray-500">New Orders</span>
        <div class="text-3xl font-extrabold text-pink-600 mt-2">42</div>
      </div>
    </div>
    
  </div>
</div>
